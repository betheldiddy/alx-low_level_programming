#include "main.h"

/**
* _isupper - check for the uppercase characters
* @c: Variable text
* Return: Alway 0.
*/
int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
